import { toRem } from '../helpers'

export const theme = {
   colors: {
      base: '#fafafa',
      dark: '#061818',
      gray: '#bababa',
      red: '#F02D3A'
   },
   global: {
      sizes: {
         navigationHeight: toRem(128)
      }
   }
}